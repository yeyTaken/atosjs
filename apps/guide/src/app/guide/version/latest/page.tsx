import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "AtosJS - Latest",
};

export default function Home() {
  return (
    <div>
      <h1>latest</h1>
    </div>
  );
}