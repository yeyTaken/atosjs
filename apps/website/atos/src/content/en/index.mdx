---

title: "AtosJS - Home"

---

import { Callout, Steps, Tabs, FileTree } from "nextra/components";
import { FaFeatherAlt, FaCogs } from "react-icons/fa";

import Home from "@/components/en/Home";

<Home />

<div style={{ display: 'flex', alignItems: 'center', gap: '0.5rem', fontSize: '2rem', fontWeight: 'bold' }}>
  <FaCogs size="3rem" />
  <span>Quick Setup.</span>
</div>

<Tabs items={['JavaScript', 'TypeScript', 'CLI']}>
  <Tabs.Tab>
  <Steps>
    <Callout type="info">
      Installing AtosJS
      <Tabs items={['npm', 'pnpm', 'yarn', 'bun']}>
        <Tabs.Tab>
          ```bash filename="Terminal" copy
          npm install atosjs@latest
          ```
        </Tabs.Tab>

        <Tabs.Tab>
          ```bash filename="Terminal" copy
          pnpm add atosjs@latest
          ```
        </Tabs.Tab>

        <Tabs.Tab>
          ```bash filename="Terminal" copy
          yarn add atosjs@latest
          ```
        </Tabs.Tab>

        <Tabs.Tab>
          ```bash filename="Terminal" copy
          bun add atosjs@latest
          ```
        </Tabs.Tab>
      </Tabs>
    </Callout>

    ### Configuration File (`atos.config.js`):

    ```javascript filename="atos.config.js" showLineNumbers copy
    const { GiftManager, TimeFormat } = require("atosjs");

    module.exports = {
      gift: new GiftManager({
        logging: false, // Disables logs
        atosdb: { filePath: "gifts.sqlite" }, // default filePath: "json.sqlite"
      }),

      t: new TimeFormat(),
    };
    ```

    ### Main File (`index.js`):

    ```javascript filename="index.js" showLineNumbers copy
    const { gift } = require("./atos.config");

    (async () => {
      const giftCoin = await gift.generate({
        type: "coin",
        value: 100,
      });

      console.log({
        GiftId: giftCoin,
        log: await gift.view(giftCoin),
      });
    })();
    ```

    ### File Structure:
    <FileTree>
      <FileTree.Folder name="quick-setup" defaultOpen>
        <FileTree.File name="atos.config.js" />
        <FileTree.File name="index.js" />
        <FileTree.File name="gifts.sqlite" />
        <FileTree.File name="package.json" />
      </FileTree.Folder>
    </FileTree>

    </Steps>
  </Tabs.Tab>

  <Tabs.Tab>
    <Steps>
      <Callout type="info">
        Installing AtosJS
        <Tabs items={['npm', 'pnpm', 'yarn', 'bun']}>
          <Tabs.Tab>
            ```bash filename="Terminal" copy
            npm install atosjs@latest
            ```
          </Tabs.Tab>

          <Tabs.Tab>
            ```bash filename="Terminal" copy
            pnpm add atosjs@latest
            ```
          </Tabs.Tab>

          <Tabs.Tab>
            ```bash filename="Terminal" copy
            yarn add atosjs@latest
            ```
          </Tabs.Tab>

          <Tabs.Tab>
            ```bash filename="Terminal" copy
            bun add atosjs@latest
            ```
          </Tabs.Tab>
        </Tabs>
      </Callout>

      ### Configuration File (`atos.config.ts`):

      ```ts filename="atos.config.ts" showLineNumbers copy
      import { GiftManager, TimeFormat } from 'atosjs';

      export const gift = new GiftManager({
        logging: false, // Disable logs
        atosdb: { filePath: "gifts.sqlite" }, // Default filePath: "json.sqlite"
      });

      export const t = new TimeFormat();
      ```

      ### Main File (`index.ts`):

      ```ts filename="index.ts" showLineNumbers copy
      import { gift } from '@atosjs';

      (async () => {
        const giftCoin = await gift.generate({
          type: "coin",
          value: 100,
        });

        console.log({
          GiftId: giftCoin,
          log: await gift.view(giftCoin),
        });
      })();
      ```

      ### tsconfig:
      ```json {6} filename="tsconfig.json" showLineNumbers copy
      {
        "compilerOptions": {
          "baseUrl": "./",
          "paths": {
            "@/*": ["src/*"],
            "@atosjs": ["./atos.config.ts"]
          },
          "outDir": "./dist",
          "rootDir": "./src",
          "strict": true,
          "module": "commonjs",
          "target": "es6",
          "esModuleInterop": true
        }
      }
      ```

      ### File Structure:
      <FileTree>
        <FileTree.Folder name="quick-setup" defaultOpen>
          <FileTree.Folder name="src" defaultOpen>
            <FileTree.File name="index.ts" />
          </FileTree.Folder>
          <FileTree.File name="atos.config.ts" />
          <FileTree.File name="gifts.sqlite" />
          <FileTree.File name="package.json" />
          <FileTree.File name="tsconfig.json" />
        </FileTree.Folder>
      </FileTree>

    </Steps>
  </Tabs.Tab>

  <Tabs.Tab>Under development...</Tabs.Tab>
</Tabs>
