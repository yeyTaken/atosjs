import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "AtosJS - Inicio",
};

export default function Home() {
  return (
    <div>
      <h1>Hello, World!</h1>
    </div>
  );
}