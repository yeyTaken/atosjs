const { GiftManager, TimeFormat, ms } = require('atosjs');
require('dotenv/config');

const gift = new GiftManager({
    mongodb: {
        connect: process.env.MONGO_URI,
        dbName: 'gift',
    }
});

module.exports = { gift, TimeFormat, ms };