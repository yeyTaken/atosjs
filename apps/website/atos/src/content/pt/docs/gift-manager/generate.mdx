---
title: "AtosJS - generate "
---

import { Callout, Tabs, Steps, FileTree } from "nextra/components";
import { FaPlugCircleCheck } from "react-icons/fa6";

<Tabs items={['Parâmetros', 'Exemplo']}>

<Tabs.Tab>
<Steps>

### `type`

<Callout type=".">
Define o tipo do presente que será gerado, como por exemplo `coin`, `desconto`, `premium`, etc.
</Callout>

```javascript filename="index.js" showLineNumbers copy 
const { GiftManager } = require("atosjs");
const gift = new GiftManager();

(async () => {
    const coin = await gift.generate({
        type: 'coin', // O 'type' geralmente é usado para definir o nome do presente.
    });

    console.log({
        code: coin,
    });

})();
```

### `value`

<Callout type=".">
Determina o valor associado ao presente. Pode ser um número, string ou até mesmo um objeto personalizado.
</Callout>

```javascript filename="index.js" showLineNumbers copy
const { GiftManager } = require("atosjs");
const gift = new GiftManager();

(async () => {
    const coin = await gift.generate({
        value: 1500, // O 'value' é usado para definir o valor do presente.
    });

    console.log({
        code: coin,
    });

})();
```

### `expiration`

<Callout type=".">
Define em quanto tempo o presente irá expirar após ser criado. Aceita formatos como `30s`, `7d`, `1y`, entre outros.
</Callout>

```javascript filename="index.js" showLineNumbers copy
const { GiftManager } = require("atosjs");
const gift = new GiftManager();

(async () => {
    const coin = await gift.generate({
        expiration: '30d', // O 'expiration' é usado para definir o tempo de válidade do presente.
    });

    console.log({
        code: coin,
    });

})();
```

### `maxRedeem`

<Callout type=".">
Define o número máximo de vezes que o presente pode ser resgatado antes de se tornar inválido.
</Callout>

```javascript filename="index.js" showLineNumbers copy
const { GiftManager } = require("atosjs");
const gift = new GiftManager();

(async () => {
    const coin = await gift.generate({
        maxRedeem: 12, // O 'maxRedeem' é usado para definir a quantidade de uso do presente.
    });

    console.log({
        code: coin,
    });

})();
```

### `edit`

<Callout type=".">
Permite personalizar o código do presente em vez de gerar um código aleatório automaticamente.
</Callout>

```javascript filename="index.js" showLineNumbers copy
const { GiftManager } = require("atosjs");
const gift = new GiftManager();

(async () => {
    const promotion = await gift.generate({
        edit: { code: "NATAL2025" }, // O 'edit' é usado para definir um código específico em vez de gerar um código aleatório.
    });

    console.log({
        code: promotion,
    });

})();
```

</Steps>
</Tabs.Tab>

<Tabs.Tab>

<Callout type=".">
Exemplo geral do `await gift.generate();{:js}`
</Callout>

```javascript filename="index.js" showLineNumbers copy
const { GiftManager } = require("atosjs");
const gift = new GiftManager();

(async () => {
  const coin = await gift.generate({
    type: "coin", // tipo do presente, no caso, moeda.
    value: 30000, // valor do presente, no caso, 100 moedas.
    expiration: '7d' // duração de 7 dias.
  });

  console.log({
    code: coin, // código do presente gerado.
    log: await gift.view(coin) // visualização do presente gerado.
  });
})();
```

<Callout type=".">
<div style={{ display: 'flex', alignItems: 'center', gap: '0.5rem', fontSize: '1rem', fontWeight: 'bold' }}>
  <FaPlugCircleCheck size="1rem" />
  <span>Resultado esperado:</span>
</div>

```bash filename="Terminal"
✔ Connected to 🚀 AtosDB (json.sqlite)                                                                                          12:55:47 PM
{
  code: 'LTIDDD8E1L23P',
  log: {
    valid: true,
    type: 'coin',
    value: 100,
    maxRedeem: 1,
    redeemedCount: 0,
    expiresAt: '2025-05-04T12:55:47.794Z'
  }
}
```
</Callout>

</Tabs.Tab>
</Tabs>


<Callout type=".">
> Resumo geral do `await gift.generate();{:js}`
</Callout>

| Opção       | Tipo       | Padrão        | Descrição                                |
| ----------- | ---------- | ------------- | ---------------------------------------- |
| `type`   | `string`   | `undefined`         | Define o tipo do presente. **Exemplo**: `desconto`, `premium`, `moedas`. |
| `value`   | `unknown`    | `undefined`    | Define o valor do presente: **Exemplo**: `100`, `'VIP Access'`, `{ wallet: { coins: 5000 } }`. |
| `expiration`   | `string`    | `undefined`  | Define a data de expiração do presente. **Exemplo**: `'60s'` (expira em 60 segundos), `'7d'` (expira em 7 dias), `'1y'` (expira em 1 ano). |
| `maxRedeem`    | `number`    | `undefined`  | Limita o número de vezes que o presente pode ser resgatado. O valor padrão é `1`. **Exemplo**: `5` (permite até 5 resgates). |
| `edit`    | `{ code: string }`    | `undefined`  | Se você quiser customizar o código do presente. **Exemplo**: `{ code: 'NATAL2025' }` — irá gerar um presente com o código `NATAL2025`. |
